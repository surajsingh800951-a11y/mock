# Mock Interview Questions — Python Basics + Git/GitHub
### (For 8 students — each will interview one another)

**Topics covered:**
- Python Introduction
- Variables
- Data Types
- Git & GitHub Basics

**How to use:** Each student is given one Set (Set 1 to Set 8). The interviewer for that student will ask only from that set. A short "expected answer point" is given with each question so the interviewer can judge whether the answer is correct. The answer doesn't need to match word-for-word — the concept should just be clear.

---

# Magan
 
1. **What is Python and where is it used?**
   *Expected: A high-level, interpreted, easy-to-read programming language; used in web development, data science, automation, AI/ML.*

2. **What is a variable?**
   *Expected: A name used to store a value in memory.*

3. **What are the rules for naming a variable in Python?**
   *Expected: Must start with a letter or underscore, not a number; no spaces allowed; case-sensitive; cannot use reserved keywords.*

4. **What is the difference between `x = 10` and `x = "10"`?**
   *Expected: The first is an integer, the second is a string — they look the same but have different types.*

5. **How many basic data types does Python have? Name them.**
   *Expected: int, float, str, bool, list, tuple, dict, set.*

6. **What does the `type()` function do?**
   *Expected: It tells you the data type of a variable.*

7. **What is Git?**
   *Expected: A version control system that tracks changes in code.*

8. **What is the difference between Git and GitHub?**
   *Expected: Git is a tool (local), GitHub is an online platform where Git repositories are hosted.*

9. **What does the `git init` command do?**
   *Expected: Turns the current folder into a Git repository.*

10. **What is a repository (repo)?**
    *Expected: A folder/project that stores code along with its history.*

---

# Shivam Yadav

1. **Is Python an interpreted or compiled language?**
   *Expected: Interpreted — it executes line by line.*

2. **What symbol is used to assign a value in Python?**
   *Expected: `=` (equal sign).*

3. **What's the difference between Integer and Float? Give an example.**
   *Expected: Integer is a whole number (5), Float is a decimal number (5.5).*

4. **What is a String? How do you write it?**
   *Expected: Text data; written in single or double quotes — "hello" or 'hello'.*

5. **What does the Boolean data type represent?**
   *Expected: A True or False value.*

6. **How can you change the data type of a variable (type conversion)?**
   *Expected: Using functions like int(), float(), str().*

7. **What is the `git clone` command used for?**
   *Expected: To bring a copy of a remote repository onto your local system.*

8. **What does the `git add` command do?**
   *Expected: Adds changes to the staging area, preparing them for commit.*

9. **What does `git commit` do?**
   *Expected: Permanently saves the staged changes to history, along with a message.*

10. **What is the difference between a public and private repository on GitHub?**
    *Expected: Public is visible to everyone, private is only visible to authorized people.*

---

# Suraj Singh

1. **What do you need to run Python code?**
   *Expected: A Python interpreter installed on the system.*

2. **How do you write a comment in Python?**
   *Expected: Using the `#` symbol for a single line comment.*

3. **What's the difference between a List and a Tuple?**
   *Expected: A List is mutable (can be changed) `[]`, a Tuple is immutable `()`.*

4. **What does a Dictionary data type store?**
   *Expected: Key-value pairs, e.g. `{"name": "Raj", "age": 20}`.*

5. **What is the `input()` function used for?**
   *Expected: To take input from the user, and it always comes in as a string.*

6. **Can a variable name start with a number (e.g. `1name`)?**
   *Expected: No, it will throw an error.*

7. **What does the `git status` command show?**
   *Expected: Which files are modified/staged/untracked — the current state.*

8. **What's the difference between `git push` and `git pull`?**
   *Expected: Push sends local changes to the remote (GitHub); pull brings changes from remote into local.*

9. **What is a branch in Git?**
   *Expected: A separate line of development where you can work without disturbing the main code.*

10. **What is the purpose of a README file in a GitHub repo?**
    *Expected: Shows information/documentation about the project.*

---

## Dileep

1. **Python is case-sensitive — what does that mean?**
   *Expected: `Name` and `name` will be treated as two different variables.*

2. **How do you assign multiple variables in one line?**
   *Expected: `a, b, c = 1, 2, 3`*

3. **How is a Set different from a List?**
   *Expected: A Set does not allow duplicate values and is unordered `{}`.*

4. **What is the `len()` function used for?**
   *Expected: Returns the length (count of items) of a string/list/tuple.*

5. **What does the None type represent in Python?**
   *Expected: No value / empty value.*

6. **What does "dynamic typing" mean in Python?**
   *Expected: You don't need to declare a variable's type — it's decided automatically based on the value.*

7. **What does the `git log` command show?**
   *Expected: The history of all previous commits.*

8. **When does a merge conflict happen?**
   *Expected: When two branches have different changes on the same line/file and Git can't decide which to keep.*

9. **What does "forking" mean on GitHub?**
   *Expected: Creating your own copy of someone else's repository under your account.*

10. **What is a Pull Request (PR)?**
    *Expected: A request to merge your changes into the original repository.*

---

## Satyam

1. **Why is Python considered a beginner-friendly language?**
   *Expected: Simple syntax, readable like English, less code needed to get things done.*

2. **Can a variable be reassigned? Give an example.**
   *Expected: Yes, `x = 5` then `x = "hello"` — both value and type can change.*

3. **Among `str`, `int`, `float` — which one is mutable?**
   *Expected: None of them — they are all immutable.*

4. **What is a complex data type in Python? (basic idea)**
   *Expected: A combination of real and imaginary numbers, e.g. `2+3j`.*

5. **How do you store a boolean value in a variable?**
   *Expected: `flag = True` or `flag = False`*

6. **What does the `print()` function do?**
   *Expected: Displays output on the screen.*

7. **What is a `.gitignore` file used for?**
   *Expected: To list files/folders that Git should not track.*

8. **What's the difference between a local repository and a remote repository?**
   *Expected: Local is on your own computer, remote is on a server like GitHub.*

9. **What is `git checkout` (or `git switch`) used for?**
   *Expected: To switch branches or move to a specific commit.*

10. **Why is writing a commit message important?**
    *Expected: So it's clear why that change was made, making the history easier to understand.*

---

## Vishu

1. **What is the file extension for a Python file?**
   *Expected: `.py`*

2. **Is underscore (`_`) allowed in variable names?**
   *Expected: Yes, e.g. `my_name`*

3. **What is type casting?**
   *Expected: Converting one data type into another, e.g. `int("5")`*

4. **What will `bool(0)` and `bool(1)` output?**
   *Expected: `False` and `True`*

5. **What is string concatenation?**
   *Expected: Joining two strings using `+`, e.g. `"Hi" + "There"`*

6. **Can a list hold different data types inside it?**
   *Expected: Yes, e.g. `[1, "hello", True]`*

7. **What does the `git branch` command do?**
   *Expected: Creates a new branch or lists existing branches.*

8. **What is the staging area in Git?**
   *Expected: The place where changes are kept before committing (after `git add`).*

9. **What does "Star" mean on GitHub?**
   *Expected: Bookmarking/liking a repository, showing appreciation.*

10. **What is the `git diff` command used for?**
    *Expected: To show what changes have been made in files.*

---

## SET 7

1. **Is Python a general-purpose or specific-purpose language?**
   *Expected: General purpose — used across multiple domains.*

2. **Can you use a keyword as a variable name (e.g. `if = 5`)?**
   *Expected: No, keywords are reserved.*

3. **Give an example of a float value.**
   *Expected: `3.14`, `0.5`, etc.*

4. **How do you create an empty list?**
   *Expected: `my_list = []`*

5. **Why would you use a Tuple when a List already exists?**
   *Expected: When the data should not change (fixed/constant data), a tuple is used.*

6. **What does the `id()` function tell you about a variable?**
   *Expected: The variable's memory address.*

7. **What do you need to create a GitHub account?**
   *Expected: An email ID and a username/password.*

8. **What does `git remote add origin <url>` do?**
   *Expected: Connects the local repo to a remote (GitHub) repository.*

9. **Why is a version control system important?**
   *Expected: To track code history, enable team collaboration, and roll back to a previous version if something goes wrong.*

10. **What's the difference between a cloned repo and a newly created repo?**
    *Expected: A cloned repo is a copy of an existing repo (with history), a new repo starts completely fresh.*

---

## SET 8

1. **Why is Python called a "readable" language?**
   *Expected: Its syntax is simple like English, and it uses indentation-based structure.*

2. **How do you print the value of a variable?**
   *Expected: `print(variable_name)`*

3. **Which function is used to check a data type?**
   *Expected: `type(variable_name)`*

4. **How do you access a value in a dictionary?**
   *Expected: Through its key, e.g. `dict["key"]`*

5. **What happens if you add a duplicate value to a Set?**
   *Expected: It gets automatically removed — only unique values remain.*

6. **What are `int`, `float`, `bool` collectively called?**
   *Expected: Numeric/primitive data types.*

7. **What does `-m` mean in `git commit -m "message"`?**
   *Expected: A flag to write the commit message directly with the commit.*

8. **What's the advantage of a central repository (GitHub) in team projects?**
   *Expected: Everyone can work on the same code, and changes stay in sync.*

9. **What is the `master`/`main` branch?**
   *Expected: The default/primary branch that holds the stable code.*

10. **In which format is the README.md file written?**
    *Expected: Markdown format (.md)*

---

## Quick Notes for the Interviewer (common for all sets)

- Each set has **10 questions** — Python intro/variables/data types (6) + Git/GitHub (4).
- Suggested time limit: 8–10 minutes per interview.
- If a student can't answer, give one hint, then move to the next question.
- Don't expect word-for-word matching — the concept being correct is what matters.
